# bibleit python

Interactive Bible Python reading from terminal.

## How to install

```shell
pip install bibleit
```

## How to run

```shell
python -m bibleit
```

Also, you can serve the app in the browser:

```shell
python -m bibleit.serve
```
